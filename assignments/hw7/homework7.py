avg_acc=0
weigh_acc = 0
class_acc = 0
for line in infile:
    new_line=line.split()
    for i in range (2,len(new_line), 2)
        avg= new_line[i]*new_line[i+1]
        avg_acc = avg_acc + avg
        weigh_acc =  weigh_acc + new_line[i+1]
        if (weigh_acc > 100):
            outfile.write("...")
        elif(weight < 100):
            outfile.write("..")
        class_size+= 1
        class_acc=class_acc + avg_acc


        def squareEach(nums):
            for i in range(len(nums)):
                nums[:] = nums ** 2


        def listSum(nums):
            acc = 0
            for i in range(len(nums)):
                acc += nums[:]
                return acc


        def circleoverlap():
            win = GraphWin("Circle", 400, 400)
            p1 = win.getMouse()
            p2 = win.getMouse()
            r = math.sqrt((p1.getX() - p2.GetX() ** 2 + (p1.getY) - p2.getY() ** 2)
            c1 = Circle(p1, r)
            c1.draw(win)


        def returnNumbers()
